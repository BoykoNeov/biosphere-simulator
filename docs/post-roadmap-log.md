# Post-roadmap work log

**The roadmap has no Phase 10 — everything here was chosen, not scheduled.** One row
per piece of work, newest concerns last; each names its own plan doc, which is the
fuller record (`docs/plans/post-roadmap-*.md`).

**Why this file exists.** These rows lived in `CLAUDE.md` until 2026-08-11, where they
had grown to ~206 KB — about 50k tokens loaded into *every* session, against a file
whose own opening line calls itself "a map, not a record". The rows were moved here
**verbatim**, byte-for-byte, rather than summarised: condensing 23 rows by hand is
exactly how a finding gets silently dropped.

**That fix did not hold, and this file now carries both halves.** `CLAUDE.md` kept a
one-line index row per entry — and those rows averaged 243 bytes, so the index re-grew
14.4 KB → 17.7 KB *the same day* it was cut. Moving the record without changing the rule
that generates rows only bought a few weeks. On 2026-08-12 the index moved here too,
again verbatim, and `CLAUDE.md` now points at this file instead of summarising it. The
diagnosis and the rules are in `docs/context-budget.md`; the size ceiling has a paired
test (`tests/test_context_budget.py`), because every other contract in this repo has one
and this one had re-bloated inside 24 hours without it.

**And the record then had to leave this file too, on 2026-08-12.** Not for size — for
shape: one work item was one physical *line* of a markdown table, up to 54,343 characters
of it, which defeats `Grep`, `Read` and `git diff` alike. The record now lives one file
per item in `docs/log/`, moved verbatim and digest-checked; the table below is pointers.
That is rule 4 of `docs/context-budget.md`.

So: **this file is the index, plus a pointer table into `docs/log/`.** New work adds one
line to the index, one pointer row, and one file in `docs/log/` — and adds nothing to
`CLAUDE.md`. The paired test fails if a row exists on one side and not the other.

⚠ **One plan doc on disk is deliberately not in the index below: `post-roadmap-direction.md`.**
It is the only *forward-looking* plan here — the consolidated read of the whole open queue,
written 2026-08-13 — so it has no finished work behind it and therefore no file in
`docs/log/`. Naming it in the index would break the index↔record pairing for the right
reason and the wrong way, so it is named here instead. **When its items are executed they
each earn the normal three (index line, pointer row, record file) and this paragraph goes
with the doc.** Anything else on disk matching `post-roadmap-*.md` and missing from the
index is a bug, and `test_every_plan_doc_is_indexed` still catches it.
⚠ One consequence, found the hard way on 2026-08-13 when the first of its items was
executed: **a file in `docs/log/` must not name this doc by filename either.** The
plan-doc parity check reads the index *section* on one side and the union of the record
files on the other, so a record naming it puts it on the record side only and turns that
check red. Record files refer to it as *"the direction plan"* and rely on this paragraph.

⚠ **A SECOND forward-looking plan joined it on 2026-08-14: `post-roadmap-gross-net-gas-exchange.md`**
— the continuous plant gas exchange + within-day light path work (planned, nothing built,
`git diff src/` empty). Same shape, same exemption, same two consequences: it is named *here*
rather than in the index, and a file in `docs/log/` must not name it by filename while it is
exempt — call it *"the gas-exchange plan"*. **When it is built it earns the normal three and
this paragraph goes with it**; until then it is deliberately exempt rather than missed.
⚠ It was written on 2026-08-14 and left **untracked** for a day, which turned
`test_every_plan_doc_is_indexed` **red on the working tree while `main` itself was green** —
an unindexed plan doc fails the gate whether or not git is tracking it. Two lessons, and the
second is the one that bites: a plan doc is committed *with* its exemption in the same commit,
and **an exemption written for a temporary state is a deletion someone must remember** — which
is exactly what the step-unfreeze paragraph two paragraphs down records going wrong.

⚠ **A THIRD forward-looking plan joined them on 2026-08-15: `post-roadmap-value-switch-harness.md`**
— a `src/lab/` harness that substitutes param values **in memory** so an "A's number vs B's
number" comparison is a reproducible command rather than a probe script written and thrown
away (planned, nothing built, `git diff src/` empty). Same shape, same exemption, same two
consequences: named *here* rather than in the index, and a file in `docs/log/` must not name it
by filename while it is exempt — call it *"the value-switch plan"*. **When it is built it earns
the normal three and this paragraph goes with it.** ⚠ Written and committed *with* this
paragraph, per the lesson recorded directly above.

⚠ **A FOURTH exemption existed for exactly one day and is GONE (2026-08-16).** It covered
`post-roadmap-reference-flip.md`, written the day before as a plan-only doc with nothing built.
**Slice 1 landed the next day and ended the premise** — the exemption's own rationale is *"no
finished work behind it and therefore no file in `docs/log/`"*, which stopped being true — so the
doc took the normal three (index line, pointer row, record file) then rather than carrying a
false exemption through ten more slices. Its plan doc had assigned that deletion to slice 11;
that was rewritten too. ⚠ **The general rule this makes explicit: an exemption expires when the
first slice of its plan lands, not when the last one does.** A doc under construction is not
forward-looking, and the paragraph below is why a stale exemption is worth deleting early.

⚠ **The step-unfreeze exemption is GONE (2026-08-14), because the ceremony landed.** A
paragraph here used to exempt `post-roadmap-step-unfreeze.md` from the index while its work
was in progress, ending *"when the ceremony lands it earns the normal three — index line,
pointer row, record file — and this paragraph goes with it."* It has the normal three now, so
the paragraph went. ⚠ Worth one line of its own: the landing commit added the **pointer row
and the record file and not the index line**, and left this paragraph standing — so the three
checks stayed red for five commits while the batch read them as "pre-existing". *An exemption
written for a temporary state is a thing someone must remember to delete; the deletion is part
of the work it exempts, not paperwork after it.*

⚠ Prose elsewhere in the tree still points at "the CLAUDE.md status row" (the
compose-gap discipline: *re-read the status row for the bucket you closed*). That rule
survives both moves; **its target is now the item's file in `docs/log/`** — the index line
and the pointer row here are navigation, not the record.
`src/domains/power/params/self_discharge.yaml`
also refers to something "CLAUDE.md left on the table" — deliberately **not** edited,
because a comment-only edit to a param file moves its manifest hash and is an unfreeze
event; a pointer fix is not worth a freeze ceremony.

## Index — one line per row, in the same order

Navigation only; **the record is one file per row in `docs/log/`.** This index moved here verbatim
from `CLAUDE.md` on 2026-08-12, under the retirement rule in `docs/context-budget.md`:
it was 7.3 KB loaded into *every* session, to index a file most sessions never open.

| Work | Status | Detail |
|---|---|---|
| Development posture — the Rust-primary pivot | **DECIDED 2026-07-20: Option A** — new content is Rust-first; Python stays frozen-canonical and green for validated science | `post-roadmap-rust-primary-pivot.md` |
| Scope (B): the day-neutral habitat crop | COMPLETE 2026-07-20 — diagnosed against an offline LINTUL3 oracle; authored, not validated; no golden moved | `post-roadmap-day-neutral-crop.md` |
| Scope (B): decomposer calibration | COMPLETE 2026-07-21 — the carbon rates moved above-range → top-of-range; biosphere re-frozen. Closure requires the fast edge | `post-roadmap-decomposer-calibration.md` |
| The nitrogen-cycle FORM gap | (A)+(B) **BUILT** 2026-07-27; (C) **REFUSED**; (D) **not buildable as recorded**. Two weakly-sourced params retired by changing the form, not by finding a citation | `post-roadmap-nitrogen-cycle-form.md` |
| The canopy regulator | DIAGNOSED 2026-07-27, **NOT BUILT** — fixes the canopy and is bit-identically inert on every frozen scenario | `post-roadmap-canopy-regulator.md` |
| The chamber-scale diagnosis | DIAGNOSED 2026-08-09 — the sealed jar holds ~2 days of one crop's carbon; enlarging it is refuted by BVAD. The defect is the gate, not the chamber | `post-roadmap-chamber-scale.md` |
| The acceptance gate | DIAGNOSED 2026-08-09 — the six tightest margins in the whole roster are one stock in one rig; `open_season`'s carbon source is unclamped and holds 0.0. **Its finding 6 ("the decision is the user's") was ADJUDICATED the same day — see the next row** | `post-roadmap-acceptance-gate.md` |
| The science assertions get contract standing | COMPLETE 2026-08-09 — `science_bands` + `liveness_floors` in both manifests; a schema unfreeze, no value moved | `post-roadmap-acceptance-gate-standing.md` |
| The humification split (a CUE) | **BUILT 2026-08-10** — CENTURY's partition; the settling transient grew ~3 yr → ~35, past the frozen horizon | `post-roadmap-cue-humification.md` |
| Soil carbon pool fractionation | **REFUSED TWICE** (2026-08-10) — `decomposition_rate` measured un-retirable by the only cited alternative on the shelf | `post-roadmap-soil-fractionation.md` |
| The crew-coupled loop | TAKEN and **REFUSED** 2026-08-10 — a chamber is carbon-limited by *isolation*, not volume; the two-rate split caps the crop at the standing pool | `post-roadmap-crew-coupled-loop.md` |
| The decade CO₂ guard, re-anchored | COMPLETE 2026-08-10 — the window was measured inert on the frozen tree and removed; a tightening, not a re-tune | `post-roadmap-co2-guard-reanchor.md` |
| Stem-reserve remobilization | REFUSED 2026-08-10 (provenance), **BUILT 2026-08-12 on the user's call** — the stem feeds the seed, and **stops at maturity**: [E]'s own `FINISH DS = 2.` is a *domain boundary*, not a cessation rule. The native mirror caught a **scenario constant** the Python suite could not see | `post-roadmap-stem-reserves.md` |
| Test-suite runtime (tooling, not science) | COMPLETE 2026-08-09 — `-n 12`, below-normal priority class; whole suite 7m05s | `docs/test-suite-runtime.md` |
| The first authored habitat | COMPLETE | `post-roadmap-authored-habitat.md` |
| The second authored habitat (`bioregenerative_station`) | COMPLETE 2026-08-11 — a frozen flow's "boundary" wiring field is a **name, not a constraint**, so the calibrated equipment recycles | `post-roadmap-bioregenerative-station.md` |
| Tier 1: grow the flow registry | COMPLETE — authoring platform unfrozen (`flow_types` 3→12, `param_loaders` 2→5) | `post-roadmap-flow-registry-growth.md` |
| Tier 2: the grammar — `monod` | COMPLETE — grammar unfrozen (`expr_nodes` 7→8); saturation is now sayable | `post-roadmap-grammar-monod.md` |
| Bucket 3 (A): diagnose + pin the oracle gap | COMPLETE — the gap is **structural**, so the deferred quantitative match is not a calibration task | `post-roadmap-validation.md` |
| Bucket 3 (B): the full oracle match | INCREMENT 1 + CEREMONY 2 COMPLETE 2026-07-20 — vernalization + photoperiod shipped. **The oracle is a diagnostic, never a fit target** | `post-roadmap-oracle-match.md` |
| Bucket 2: the export-fidelity hazard | COMPLETE — `rationed == 0` is not "the export is right"; closed by multi-rate Step 5's build-time `k·h < 1` precondition | `tests/test_authoring_export_fidelity.py` |
| Bucket 2: multi-rate authoring | COMPLETE — all 7 steps; the authoring platform re-frozen with the multi-rate surface in it, both ports | `post-roadmap-multirate-authoring.md` |
| Bucket 2: the rationing gate | COMPLETE — `run_scenario` now raises on rationing; conservation is not survival | `post-roadmap-rationing-gate.md` |
| Bucket 3 (C): cite the no-oracle params | CLOSED after 7 rounds — 8 cited, 14 design, 7 unciteable; blocked on retrieval, not effort, with the residual risk documented | `post-roadmap-citation.md` |
| The O₂ regulator's reversal | DIAGNOSED + CORRECTED 2026-08-11 — the clamp was already refused; the reversal is **not** author-only — it fires in 3 frozen runs the goldens are blind to. A sentence true when written, falsified by a seam three phases later | `post-roadmap-o2-makeup-reversal.md` |
| The direction gate (`ReversedFlowError`) | **BUILT 2026-08-11** — a third run-time verdict, `RationedError`'s sibling not its variant; authoring platform unfrozen (`flow_types` gains `demand_controlled`), both ports | `post-roadmap-o2-makeup-reversal.md` |
| Tooling: the PDF-backed citation pins (not science) | FIXED 2026-08-11 — a poppler upgrade re-wrapped quoted phrases; `sources/` is gitignored, so **CI green means nothing was checked** there | `docs/post-roadmap-log.md` (last row) |
| **Potato — the first SECOND species** | Stage 1 COMPLETE 2026-08-11 (stage 2, the Rust habitat mirror, deferred) — a crop is now a param SET; PCSE's bundled demo DB ships 6 offline oracles, so "new species = authored-only" was a stale reading. Two sources disagree qualitatively about when a tuber starts filling. **Its "one cause, two symptoms" canopy attribution was CORRECTED 2026-08-11 (measured at 39 %, not all)** | `post-roadmap-potato-crop.md` |
| The winter-wheat partition backfill | TAKEN and **REFUSED 2026-08-11** — the cited table misses the peak-LAI band 2.36×; cause isolated to root share over DVS 0–0.33. Neither table dominates the oracle: the frozen one passes because it was **fitted**. Successor named as root functional coupling — **that successor was TAKEN and its nitrogen half REFUSED the same day; see the next-but-one row** | `post-roadmap-wheat-partition-backfill.md` |
| Stem reserves: the "model FORM found" lead | **RETRACTED 2026-08-12** — the lead was a re-discovery, not a discharge: the 2026-08-10 stem-reserve exercise had already quoted [E] p. 93 §3.2.4, built the form it programs and measured it (harvest index 1.151× the oracle). *"A data table is not a model form"* was about Table 7. The stale artifact was **our own record**, not the shelf. Stem reserves stands where it stood: a provenance judgement, and the verdict is the user's | `post-roadmap-wheat-partition-backfill.md` |
| Root functional coupling (the wheat refusal's successor) | **REFUSED on the measurement, then BUILT on the user's call 2026-08-11** — [E] p. 136 decouples rooted depth from root mass **on purpose**, so root CARBON stays refused *by citation*. The depth gate is bit-identically inert (uptake is demand-bound everywhere), was recorded NOT BUILT, and the user overruled: shipped as `aux_set` 2→3 + `root_depth.yaml`, both ports, **no value moved**. Successor is soil layers, for water | `post-roadmap-root-functional-coupling.md` |
| Soil layers — the water side of root depth | **BUILT 2026-08-11** — the price ("the largest single piece considered") assumed N layers; [F] endorses **two stores**, so the design was refuted by one more sentence of a source already on the shelf. `flow_set` 20→21; the golden diff was **predicted before regeneration and held** — no C/N/O amount moved anywhere. Depth now buys a 2.5× canopy where the topsoil runs out | `post-roadmap-soil-layers.md` |
| Tooling: the context budget (not science) | COMPLETE 2026-08-12 — the 2026-08-11 cut was a **relocation, not a discipline**: the index regrew 14.4→17.7 KB *the same day*, ~239 B per finished item. Retirement rule + one unconditional index + a paired ceiling test; `CLAUDE.md` 17,715→9,520 B, nothing condensed | `docs/context-budget.md` |
| Re-deriving the soil water store from geometry | **BUILT 2026-08-12** — the recorded price was wrong both ways (the open field costs −2 % yield; every sealed chamber **dies and locks**, missing the escape by 0.5 kg), and two of the three named successors were **one mechanism**. Geometry + `FTSW` + demand-driven irrigation + drainage: **only water moved**, on every golden. `flow_set` 21→22, both ports | `post-roadmap-soil-water-rebasing.md` |
| Tooling: the record split (that rule set's own rule 4, built) | COMPLETE 2026-08-12 — the record table was 255,567 B in 32 physical *lines*, the longest 54,343 characters, which defeats `Grep`/`Read`/`git diff` alike. One file per item in `docs/log/`, **moved verbatim** — both sha-256 digests measured and recorded. The gate now caps line length, so a relocation cannot pass as the fix | `docs/context-budget.md` |
| The remaining water-stress curves (the re-basing's two named successors) | **`WSFD` BUILT + `WSFL` REFUSED 2026-08-12** — the successor list called `WSSD` a *threshold*; Table 15.1's caption calls it a **coefficient**, so the build needed no `FTSW` site at all and was smaller than its recorded price. `WSFL` refused on [F] itself: Box 16.2 applies it to the node-driven leaf-area branch and **not** to the carbon-driven one, which is the only branch we have. The first unfreeze here that **moves nothing in the manifest** — both automatic gates are blind to it | `post-roadmap-water-stress-curves.md` |
| Sink-limited leaf expansion (the `WSFL` refusal's named successor) | **BUILT 2026-08-12** — [F]'s node-driven branch built, and correct only inside its own stated domain: six growth-limited scenarios peaked at *exactly* 2.9068 (the end of the node curve) with leaves 2–15× too thin and three rationing, because [F] Ch. 9 is scoped to potential production and has **no mechanism for the atmosphere running out of carbon**. Bounded by **[E] Table 20's measured winter-wheat leaf thickness** [0.85, 1.50] — rationing to 0, thickness inside real wheat, and the Greenwood gate **passing downward** at the same phyllochron that failed it. ⚠ The envelope decides a **disagreement between two shelved sources** and cuts `WSFL`'s leverage ~400× in the one scenario that fires it. **Its named successor was TAKEN 2026-08-12 and DISCHARGED THE EXPOSURE BY REFUTING IT**: [E]'s four listing lines reduce to one ODE whose senescence terms cancel, making the shipped `[0.85, 1.50]` **forward-invariant for any growth history** — the mixture calculation's own answer, not a loose stand-in. No parameter moved, all three pre-registered predictions held, and the RK4 blocker stands with **no route on the shelf**. ⚠ **That last clause was FALSIFIED the same day — see the next row**: "no route" was inferred from the one knob that had been swept, and the blocker is a step-size bound, not a science defect | `post-roadmap-leaf-expansion.md` |
| **Allocation headroom** (the leaf blocker's verdict, re-opened as a measurement) | **DIAGNOSED 2026-08-12, NOTHING BUILT** — the question was not about leaves: does the frozen tree pass RK4 by *margin* or by *construction*? Measured on frozen `main`: open field **9.1×**, sealed jar **1.4×**, and a crop **30–70 % larger** than the leaf mechanism's runs clean, so plant size is not the trigger. The break is the **jar's carbon turnover time falling below `dt`** — the parked branch clears at `dt = 1/2` with nothing retuned. ⚠ Half of that is an arithmetic identity (`margin ∝ 1/dt`), and **a guard that goes quiet because the step shrank is finding 9's own lesson with the sign flipped**; what is *not* arithmetic is that the frozen science is already converged at `dt = 1` under RK4 (0.06 %) while shipped **Euler carries 3.2 %** the goldens have baked in. Reframed in the tree's own vocabulary: `margin ≈ 1/(k·h)`, and **the chamber carbon pool is the one withdrawal whose `k` is emergent**, so it is the only place the `k·h < 1` precondition enforced in three other places could never be written. Three routes priced (refuse / finer step, **three** freeze contracts / kinetic saturation, uncited). ⚠ Separately: on shipped Euler, **×4 chamber CO₂ leaves 4 % of margin** — a live fragility with nothing to do with leaves | `post-roadmap-allocation-headroom.md` |
| **CO₂ enrichment**, priced on the shipped integrator (the headroom diagnosis's successor 3) | **DIAGNOSED 2026-08-13, NOTHING BUILT** — the recorded "a fifth of the headroom per doubling" was a chord through three points; the real sweep is **non-monotone** and rations only at 1785 ppm, above where a realism move lands. The finding is elsewhere: the crop **cannot** draw the chamber below the CO₂ compensation point (`Γ*/ci_ratio = 61.07 ppm`), and RK4 pins the season low at **~76 ppm at every enrichment level** — while shipped Euler at `dt = 1` **collapses it 57.9 → 12.1 ppm**, fixing carbon where the model says it fixes none. ⚠ Unlike the margin, this **converges** rather than scaling with `dt` (57.9 → 75.1 → 75.8), so it is a statement about the *answer*, not the guard. ⚠ The **frozen ambient** run is already **24 % low** on that quantity — and crosses the shutoff, though only by 5 % there (3× under enrichment) — while **overstating** the enrichment payoff by 4.3 %. Verdict: *enrichment fails scientifically ~600 ppm before it fails numerically, and the margin is the wrong alarm* | `post-roadmap-co2-enrichment-margin.md` |
| **The CO₂ setpoint controller, priced** (the direction plan's Step 0, axis 2 — the axis that could have cancelled the step gate) | **DIAGNOSED 2026-08-13, NOTHING BUILT** — probe only, two controls first (the no-op clamp is bit-identical to `run_perennial` on all three chambers; the live clamp migrates the binding day). ⚠⚠ **The ambient control inverted the hypothesis**: holding the sealed chamber at the 357 ppm it already starts from is **four times worse** than letting it deplete (margin 1.3072 → 0.2977, season-low CO₂ 57.9 → 10.6 ppm, rationing 0 → 270), because the uncontrolled chamber **self-limits** and a controller removes that feedback — the argument needed the stock to be *large*, not *steady*. **No realistic setpoint clears, on any of the three chambers**: across them the 1000–1200 ppm band runs margin **0.41–0.95** with 3–252 silent firings and season-low CO₂ **39–77 % below** the compensation point; first pass is ~3000 ppm on the two plant-only chambers and 1500 on the consumer chamber, whose crew carries part of the supply (⚠ its margins are **censored above 2.0** — a CO₂-independent `water_vapor` event floors the reported run-minimum; every *failing* row binds on `carbon_pool`, so the verdict does not rest on one). And the discriminator — a convergence check **within** the setpoint, because the case against the step is a truncation error, not a threshold crossing — says **the controller doubles the step price**: controlled `dt = 1` at 1200 ppm is **22 % low on peak leaf carbon against its own converged limit**, against 3.2 % frozen, at the **same 4× refinement** (the two were not the same statistic as first written; re-read like-for-like the ratio is 6.9×). Where the sealed chamber clears at `dt = ½` uncontrolled, controlled at 1200 ppm it needs `dt = ¼` — ⚠ *"clears at `dt = ½`"* is itself a **sealed-chamber** number, and establishing it across all 25 scenarios is what axis 1 is for. ⚠ It also makes **RK4 at `dt = 1` unbuildable** (hard error at every setpoint), and generalizes past the probe: arbitration credits **no same-step inflows**, so a make-up *flow* is strictly worse than this dawn clamp, which is therefore the **upper bound on any controller**. Verdict: the *"may dissolve this whole gate"* warning is **refuted**, the conditional `dt = ½` recommendation is **discharged and stands**, and the controller moves back behind the gate as a `dt = ¼` object | `post-roadmap-co2-controller.md` |
| **The step sweep** (the direction plan's Step 0, axis 1 — the last input to the step decision) | **MEASURED 2026-08-14, NOTHING BUILT; the decision is the user's and is still open.** Probe only on frozen `main` (`git diff src/` empty, no golden regenerated); the parked leaf branch measured from a **git worktree**, and every inherited anchor reproduced exactly first — plus an independent cross-check the harness had never seen (the ECLSS margin reads **16.667 = 1/0.06**, the `k·dt = 0.06` design point). ⚠ **The axis is a REFINEMENT FACTOR, not an absolute `dt`**: every forcing here is a function of the integer step `n`, never `n·dt` (all five sites audited), so holding forcing fixed takes a different rule per site. **`dt = ½` clears the 61.07 ppm compensation point on all 4 sealed biosphere configurations and at all 5 enrichment levels** (sealed 57.89 → 75.06; ×5 collapses 57.89 → **12.13** at `dt = 1` but sits at 74.31 at `dt = ½`) — ⚠ **and at ×5 the shipped integrator's margin is already 0.975, below 1 today**. ⚠ Scope precisely: the criterion **only exists where the chamber is sealed** — 4 of the 8 configurations swept; the other two are open-field, and the two long horizons reproduce their 5-yr rows to every printed digit. ⚠ **THREE numbers belong in the decision, not two**: the tail statistic moves **24 %**, peak leaf carbon **4.0 %**, the headline harvest **0.7 %** — the plan's "~3 % headline" splits, and quoting the smallest undersells exactly as quoting the largest oversells. The parked leaf branch is **worse than its record shows** — a 2.2× crossing (28.02 ppm) where the frozen tree crosses by 5 % — and clears at `k·h` 0.471–0.483, i.e. 2.1× headroom. The station/physics half is nowhere near the wall (margins 10–1550×), and its one anomaly is **forcing quadrature, not integration error**: the shipped 24-sample half-sine **under-delivers solar by 0.21 %**, so refining power's step changes what the scenario *is* — recommendation, don't. ⚠⚠ **Two costs the plan does not price**: four station goldens (`greenhouse`, `lighting`, `harvest`, `sealed_station`) run through `run_master_day`, which pins the biosphere to one step per master day, so the ceremony includes a **`src/station/driver.py`** change — engine code, not contract text; and **`water_biting` converges to two different answers** (Euler harvest 0.73 vs RK4 0.005, both stable under 8× refinement, RK4's crop dead from step 1), a **new argument against every RK4 row on the menu**, suspect the self-clamping `min` at `soil_layers.py:134` that also makes its margin read exactly 1.0000 everywhere. Suite measured at **6 m 12 s** (one run, same order as the documented 7 m 05 s — **not** asserted as a correction to it); `dt = ½` is bounded by ~12 m 24 s, not pinned | `post-roadmap-step-sweep.md` |
| **The step unfreeze** — `dt = 1` → `dt = ¼` (the sweep's decision, taken; the unit was the work, not the step) | **BUILT 2026-08-14** on the user's call, and **RE-PINNED 2026-08-14** across five further commits — the first unfreeze of a **numerics** item rather than a science one, moving both contracts together (13 goldens, both manifests, the native port, the Godot consumer). ⚠ **The step was one line; the UNIT was the work**: every run length, reset period and perturbation window was written in *days* and used as *steps*, and **no test at `dt = 1` can tell a correct conversion from a wrong one, because the two are the same integer** — a green suite at the old step proves *inert*, never *correct*. Seven conflation classes found, the last of them a formula with `dt` in its **name** and missing from its arithmetic. ⚠ **A locus error rode the headline for three days across five files**: the diagnosis named the *sealed* chamber (which never crossed — 75.75 ppm at `dt = 1` in its own no-re-sow configuration) when the crossing belongs to the **perennial and consumer** chambers, and the proposed successor band inherited that locus and would have caught nothing. **Measured 2026-08-14 and now closed:** the older `57.9 ppm` behind the enrichment and controller work *did* come through the same unconditional re-sow — the sealed chamber reads **75.75 ppm without it and 57.89 ppm with it** at `dt = 1`, and crosses only in years 3–4 of a run its own golden does not perform. ⚠⚠ **The re-pin found what the flip alone did not: the sealed station no longer has a plant-limited acceptance gate.** Its binding call was the plant's draw on the shared CO₂ pool (5.0232); a quarter-step quartered it per call, so it rose to **19.0209** and crossed the ECLSS scrubber's unchanged **16.667** — same stock, different registry, and the census's headline "the six smallest live margins are `carbon_pool` in the six sealed scenarios" is now **five**. The pin that should have seen it had been reading one registry twice since the day it was written (confirmed by re-running the pre-unfreeze tree: 308.16, not 16.667). Probe 3 of the CO₂-floor guard was **re-run, not re-tuned** — the trough series rose ~35 %, so the crossing moved from above `0.8×` to between `0.68×` and `0.65×`; the floor did not move. | `post-roadmap-step-unfreeze.md` |
| **The chamber CO₂ compensation-point band** (the step unfreeze's own named successor) | **BUILT 2026-08-14** — `science_bands` 10 → 15 entries over all five chamber scenarios; `git diff src/` empty, no golden regenerated, no parameter moved, no Rust change. ⚠ **The proposal named the wrong scenario and the correction already existed**: it said *"the sealed chamber's season-low CO₂"*, the one scenario that never crossed — `step.py` had corrected that pairing days earlier and the proposal three sections away in the same freeze doc still carried the pre-correction wording. **A correction lands where it is written, not everywhere the claim was repeated.** ⚠⚠ **The tightest of the five was never measured by any work that argued about it**: every write-up quoted sealed (76.82) or perennial (75.48), while `consumer_chamber` sits **below both at 74.4210 ppm (margin 1.2186×)** and no record carried its number — the probes swept the scenarios the ARGUMENT was about, and a band is about the ROSTER. The horizon was **checked rather than assumed** (the humification split put the settling transient at ~35 yr, past every frozen horizon): to 50 yr both re-sowing chambers take their global minimum INSIDE the frozen horizon (perennial yr 2, consumer yr 5) and rise to 75.84 / 75.06, so the band's worst case is what the golden already runs. `Γ*`'s citation attempt came back **negative**, and the gap is discharged as a measured RISK rather than a debt: Teh eq. 6.19 (`Γ* = O₂/2τ`, τ = 2600) gives **57.69 ppm, BELOW the shipped 61.07**, so the shipped floor is the conservative one and closing the citation can only widen every margin — asserted as a test, and explicitly **not** a licence to swap the value (Teh's companion `Kc`/`Ko` disagree with ours, so they are different parameterizations). The bound is **derived at run time** with the literal kept only as a params tripwire, and the band is one-sided on purpose so the next mechanism's golden movement cannot force a re-pin — with the five margins pinned separately, because *an inequality that passes says nothing about how nearly it failed* | `post-roadmap-co2-compensation-band.md` |
| **The parked leaf mechanism, re-measured at the shipped step** (finding 11's named condition, discharged) | **DIAGNOSED 2026-08-14, NOTHING BUILT; the decision is the user's and is untaken.** Worktree only — `git diff src/` empty on `main`, no golden regenerated. ⚠ The rebase was checked **before** the step: the branch's 847 lines predate `step.py`, and a rate written per-step would run 4× fast **with nothing to make it red**, so the worktree was patched back to `dt = 1` first — every anchor reproduced **exactly** (peak LAI 5.2533, Greenwood W 13.6717, rationing 0, thickness max 1.2054–1.2144), and the code turns out to be step-independent **on purpose**. At the shipped step three of five evidence pieces move: peak LAI **+5.8 %**, the Greenwood margin **halves 5.2 % → 2.5 %**, thickness overshoot **improves by the PRE-REGISTERED ~4×** (0.0384 → 0.0058–0.0076), while the unclamped share **holds** at 38–46 % — ⚠ and that last one is like-for-like only because a first pass measured which branch was ENTERED (67.6 %) against a record meaning which branch DECIDED, and *landed at the top of the right range by coincidence*. ⚠⚠ **THE CONTROL INVERTS THE READING**: the frozen tree moves the same way (peak W 13.9391 → 14.1077), the mechanism sits **below** it at every step, and its effect on `open_season` runs −3.8 % → −0.26 % → −0.14 % — **the mechanism is converging toward inert exactly where its headline evidence was taken**, while staying substantial (1.27–1.32× canopy) in the carbon-limited chambers where it was not. The RK4 blocker clears (31 errors → 34 passed) and is recorded as a fact, **not an argument** — what cleared is the guard. Two spin-offs: a **stale narrative on `main`** (the frozen Greenwood margin is 2.20 %, the comment says 3.4 %; nothing went red because the pin is a band and its ratio guard sat at 0.85 against a value of 0.978 — *a characterization pin wide enough to survive the change it characterizes is decoration*), shipped separately with the guard tightened to 0.97; and the day-old CO₂ band **pricing the candidate in the contract's own units** — all five still clear, every margin −4 % | `post-roadmap-leaf-remeasurement.md` |
| **The within-day light path — the plant breathes** (the user's charge; a mechanism that was already in the tree and could not fire) | **BUILT 2026-08-14.** The charge (*"the plants MUST emit oxygen at least minute by minute and consume co2"*) lands in the **forcing**, not in a new flow: `MaintenanceRespiration`'s sealed branch already burned biomass, returned CO₂ to the pool and consumed O₂ at PQ=1 — cited, throttled, balanced — and had **never executed**, because a day-averaged PAR makes `GASS > MRES` at every step of every scenario. ⚠ Third instance of *check whether the mechanism is already in the tree and merely unreachable* — the plan's first draft had specified a new stock, flow and free parameter. PAR now follows the cited sinusoid, delivered as the **analytic mean over the step window**; the instantaneous sample was **measured and refused** (at `dt=¼` it loses 8 % of a day's carbon on one date and *gains* 4 % on another; exactly zero at `dt=1`). The day's photon dose is conserved at any step, so nothing is recalibrated. ⚠⚠ **The control is the finding**: a top-hat at the daytime mean (same daily carbon, with night) costs **1.1 %** of peak canopy while the concavity costs **14.9 %** — a ~1 % pointwise loss **compounding four-fold** because it lands on the phase that sets the canopy, so *the plan's pointwise multiplier was never a bound on the season*. ⚠ The peak-LAI band **passes at the shipped step (5.3806) and fails converged (4.7132, floor 5.0)**, measured with the band's own helper after an advisor challenge; the canopy regulator is eliminated as the fix *by measurement* (it is a 5 %/day loss above LAI 6). Carried as a KNOWN DEVIATION, not tuned. ⚠ "Every other gate moved away from danger" was this record's own first draft and is FALSE — the Greenwood margin opens (2.20 % → 4.75 %) but every CO₂ band tightens ~4–7 % and **two liveness floors narrow**, one of them to **3.2 %** of its bound on its settled value, the third move in the same direction from unrelated causes. The chamber now breathes — CO₂ up all night, O₂ its exact mirror — **and the diurnal amplitude is already converged at `dt=¼`** (0.010690 vs 0.010799 at `dt=1/32`). Cost: 13 goldens, both manifests, both reference docs, the Rust mirror (101 cross-port tests green, no band touched), **99 red tests re-pinned** — four of them *not* value moves (the nitrogen-stress bite vanished; stem-only and Table 7's top row stopped crossing Greenwood; an attractor ordering flipped a second time and was retired as an ordering; the `lighting` scenario's binding gate changed **stock**). The manifest gained `forcing.light_path`, the first **compared** hash in a `forcing` block. ⚠ Stages 0 and 2 are **not built**: a fast sub-step cannot see a light curve (`substep` keeps `n`), and rate-classing the gas is *more* expensive than a finer step for everything — so "minute by minute" is not delivered, a real day/night cycle at quarter-day granularity is. | `post-roadmap-gross-net-gas-exchange.md` |
| **The canopy this tree cannot grow** (the light path's own successor, taken on the user's call) | **DIAGNOSED 2026-08-15, NOTHING BUILT; the decision is the user's and is untaken.** `git diff src/` empty, no golden regenerated, every control reproducing its inherited anchor to the digit. ⚠⚠ **The direction plan's named candidate is REFUTED ON ITS SIGN, and the tree had said so since Phase 1**: §6b called a multi-layer canopy the cited next step that *"can move canopy assimilation UP"*, while `photosynthesis.py` has said since Step 5 that `Ag` is **concave** in PAR, so resolving depth redistributes the same photons onto a concave response and can only lower the sum — **0 of 2598 lit calls** above 1.0, and closed-loop peak LAI **5.3806 → 5.0314** shipped, **4.7132 → 4.4169** converged. Refused as a *fix*, explicitly not as science. ⚠ The **cited cheap scheme is worse than the expensive one** (the 3-point Gaussian sits below the 100-layer on every reading), and it would **not** have taken a liveness floor red — perennial's `max(tail)` moves 0.603679 → 0.603540, because the correction scales with canopy **closure** and the chambers' canopies never close: *the canopy regulator's inertness finding, verbatim, from an unrelated mechanism*. ⚠⚠ **The band's subject is a 3.5× AMPLIFIER** (×1.05 on leaf-area-per-carbon buys **+18 %** of peak LAI; on assimilation **+17 %**), so clearing the 6.1 % gap costs ~**+1.8 %** of either — ⚠ **both being the elasticity to a UNIFORM perturbation, where a closure-weighted one amplifies only ~1.14×** (the layered canopy's 5.7 % of season assimilation costing 6.5 % of peak LAI), because its correction is small exactly where the compounding would have multiplied it. Three of the inputs that set it are `TODO(cite)` provisional literals, making a ±2 % provenance disagreement the whole distance to the bound. **A band that amplifies every parameter error 3.5× cannot arbitrate between mechanisms**; this is a provenance finding, not a mechanism finding. ⚠⚠ **The SLA route has BOTH signs, and which one you get is the undocumented content of a `TODO(cite)`**: early-anchored **3.0446 (−35 %)**, late-anchored **8.2391 (+75 %)** — a 2.7× range on a question `canopy.yaml` does not answer. ⚠ And the late-anchored reading moves a **second, independently-pinned defect** the right way: peak LAI sits at **DVS 1.373** (after anthesis) against two oracles of different model families that both peak before it, and the ramp moves it to **DVS 0.958** — corroboration from an oracle the candidate knows nothing about, and the opposite of the fitted-table shape. Three options left priced and open; (1) build the layered canopy anyway at a 0.6 % band margin and (2) source the SLA anchor are **independent** | `post-roadmap-canopy-magnitude.md` |
| **The layered canopy + the leaf-thickness anchor** (the canopy diagnosis's open decision, taken on the user's call) | **BUILT 2026-08-15** — an unfreeze: 11 goldens, both manifests, one science band **restated**, the native port mirrored. Three mechanisms shipped and only two were chosen: Goudriaan's 3-point Gaussian depth integral replacing the big-leaf aggregator (closing the second half of the Jensen bias), `specific_leaf_area` **22.0 → 23.53 m²/kg** bound to Penning de Vries Table 19 "Wheat, winter" (a `TODO(cite)` retired **and the value moved +7.0 %**, so a calibration inside the goldens rather than the honour-system provenance shape), and the Van Keulen & Seligman **5 %/day mutual-shading loss above LAI 6**. ⚠⚠ **The predecessor's headline number is RETRACTED by this build**: its "the cited cheap scheme is worse than the expensive one" came from a probe whose docstring said *3-point Gaussian* and whose arithmetic was a **midpoint rectangle rule** — the real scheme tracks the 100-layer reference to **<0.2 %** where the midpoint sat 5.2 % low, so there is no quadrature error to point anywhere. **A probe that NAMES a scheme is not evidence that it IMPLEMENTED that scheme.** ⚠⚠ **The third mechanism was forced, not chosen**: the first two took peak LAI to **6.0228**, clearing the "5–8" band comfortably and tripping a *second* sourced check at `< 6.0` by 0.38 %; re-tuning 6.0 to fit was refused, so the mechanism the threshold stood in for was **built** and the band restated as *"below 6.0 **or** the loss is MODELLED"* — with the honest rider, in the test name and the manifest, that the loss is currently **inert** (6.0228 bit-identically either way). ✅ **The light path's KNOWN DEVIATION is retired**: the converged peak LAI was 4.7132, below the 5.0 floor, and now reads **5.4273** — the whole step sweep sits inside the band. ⚠ Cleared as much by **provenance** as by mechanism, exactly as the 3.5×-amplifier finding predicted, which cuts both ways for the two `TODO(cite)` literals still under it. ⚠ **The anchor was on our own shelf** — `test_potato_crop.py` already cited the same table for potato; wheat's row was one line away. Unsought corroboration: the LINTUL3 peak-LAI gap closed **22 % → 6.4 %** with nothing fitted toward it. ⚠ **A premise of the predecessor plan is false for our crop**: winter wheat's specific-leaf-weight column is **non-monotonic**, so the shipped constant is a scalar, not a DVS ramp (also because a rate-level table applied at state level is a form error). A latent **litter C:N decoupling** was surfaced and fixed, caught by a paired-recomputation test written years earlier. All 15 science gates green; the station's plant-side margin loosened 11.8868 → 12.2894; the 50-year perennial attractor now settles **below** the 0.55 the floor is anchored on — recorded, **not** re-anchored, since the gate is the 15-year reading and that still passes | `post-roadmap-canopy-magnitude.md` §7b |
| **The canopy's two uncited literals** (the layered canopy's own closing line, taken) | **`carbon_fraction` BUILT + `extinction_coef` MEASURED-NOT-BUILT 2026-08-15** — ⚠⚠ `carbon_fraction` **was never uncited**: `nitrogen.yaml` has carried the identical 0.45 bound to Raimanova et al. (2024) since 2026-07-16 under a MUST-EQUAL constraint, and the **potato crop override** has said so since 2026-08-11 while the reference file it describes still read `TODO(cite)` — fourth instance of *check your own shelf*, fourth where the stale artifact is **our own record**. Shipped provenance-only (one sha-256, no value, nothing could go red); what was **not** a copy is the **basis** — whole-plant there vs **leaf blade** here, so the root delta does not transfer and the citation binds *more* tightly than where it was written. ⚠ `extinction_coef` is a **VALUE question, not a retrieval one**: the shelf answered three times and disagreed (0.60 Penning de Vries erect-leaf/Goudriaan, 0.65 Soltani & Sinclair's *wheat* row for our exact equation, 0.68 unpublished), and "crop-specific" does not settle it since Table 10.1 lists Penning de Vries among its own sources. ⚠ **The predecessor's 3.5× amplifier does NOT transfer to this knob** — `k` raises top-of-canopy absorption and lowers penetration, self-cancelling in a closed canopy: +8.3 % on `k` buys **+0.8 %** of peak LAI shipped (**+7.3 %** converged), and the LAI peak moves **0.4 DVS earlier, from after anthesis to before it** — a second pinned defect improving. ⚠⚠ **The two gate families move in OPPOSITE directions for one reason**: all five CO₂ bands get *looser* — not safety improving but the sealed crop doing **less**, since faster interception on a **fixed** carbon inventory limits it earlier — while the perennial **liveness floor** falls from **5.12 % to 0.40 %** clearance and survives only because its statistic is a maximum over the tail (the final year, 0.525448, is **already below** the bound). **Reading the bands alone would have called `k = 0.65` uniformly safe.** 31 decade gates pass, 3 characterization pins move; three options priced, ⚠ and the two risks are **opposed** — on provenance 0.60 may be ~8 % low, on the gates 0.60 is the **conservative** side, so **no direction is safe on both axes** (and 0.40 % is inside the band where CI's libm has taken local results red). **The decision is the user's and is untaken.** ⚠ Spin-offs: `intercepted_fraction` now has **no caller in `src/` at all**; and **five frozen values in prose were superseded six commits after they were written** — the pin written for that event fired and was re-pinned *with its comment updated*, four lines below docstrings that kept the old numbers, so **a value in an assertion acquires an owner the moment it goes red and a value in prose acquires none** | `post-roadmap-canopy-provenance.md` |
| **The reference flip — Rust becomes canonical** (the Rust question re-opened; target state B, rejected in July, taken now) | **IN PROGRESS — planned 2026-08-16 in eleven slices; slices 1–8 COMPLETE (1–7 on 2026-08-16, 8 on 2026-08-17). ⚠⚠ The TARGET STATE CHANGED mid-slice-8: the user moved it from B to C — *"the whole project should become rust based, python can be used only when using external software as a reference, or in the process of rewriting"* — so Python is no longer the permanent checker but scaffolding plus the external-software oracle. The re-plan delta is drafted (plan §5b) and AWAITS THE USER; the eleven slices stand as written until then** — ⚠ the July decision (Option A, a work-type split) was measured **inert, not wrong**: of the 157 commits since, **0 touched `godot/`** and 1 touched `scenarios/`, so its Rule 1 ("new content is Rust-first") never once fired — a policy with opinions only about work that was not happening. Two of the three costs I led with were **retracted by measurement**: the goldens are not disturbed (cross-port bands `1e-11`..`1e-12` vs a tightest science gate of `4e-3`, eight orders of margin — **numerically the flip is nearly free and its whole cost is structural**), and the completeness gates **do** port (they build the registries and read them rather than introspecting a namespace: 23 derived assertions vs exactly 2 deliberately hard-coded literals). One cost confirmed (the crop-model comparison walks the season in-process, and Rust emitted only final states) and one trap found that neither the old plan nor the advisor had priced (after the flip Rust loads the params, so **Python's unit check becomes a green test guarding a path that no longer executes**). Slice 1 built the missing per-step trajectory export — full snapshots, every step, no stride knob — and its gate walks **62,272 numeric leaves** where the final-state test walked ~51. Slice 2 added the type-level `type_name()` across 58 impl sites (the first unfreeze of the Rust core) and **measured Rust's names as exactly the manifest's 23 flows / 3 aux** — the completeness contract, the thing the flip is riskiest for, is expressible before anything depends on it. Slice 3 had both ports dump their own registry inventory and gated it against the still-Python-anchored manifests: **no divergence on either** (23 flows / 3 aux, 16 flows / 0 aux), so the gate in front of slice 4 — the one that actually moves the reference — is discharged. ⚠ It shipped **two of its three planned axes**: the port has **no referent for the param-file list** (it reads no YAML, only a Python-*generated* file whose names are not filenames), so that comparison would have been the reference against itself — and the axis **cannot be re-anchored until a later slice decides who loads the params**, which inverts the plan's own dependency arrow. ⚠ The station half was included precisely because the biosphere half was already known to pass; my estimate that station would match too was **a count read off a grep by eye**, the exact evidence recorded as unreliable one day earlier. ⚠ Both earlier slices' acceptance criteria **as written were passable by a no-op** and both were widened: *an interface slice must name a call site or it accepts an interface nobody has run* — and slice 3 adds the corollary that **an empty frozen set is an inert comparison wearing the clothes of a passing one**. Slice 4 — the slice that was to move the reference — landed the census and the Rust-side regeneration path but **not a diff**, because the measurement says the reference barely has to move: **16 of the 18 goldens Rust can emit are already byte-identical** (not "inside the band" — identical, including the ~1.3 M-substep sealed station), and the 2 that differ do so by **1–2 ULP**, five orders inside their own band, diagnosed via slice 1's export as accumulated noise rather than an op-level difference. ⚠ My first measurement was taken under the wrong build profile (`--release`; the suite runs the biosphere family in debug, and both divergent cases are biosphere) — re-measured, profile is byte-neutral, but *measure a port the way the suite invokes it*. ⚠⚠ Two structural findings: swapping the divergent files in leaves **both freeze-manifest gates GREEN** — `golden_sha256` is recorded and **never compared**, so the *provenance-only edit nothing catches* covers the goldens too — while the four gates that do go red are all Windows-only, i.e. **green on CI**; and **slices 4 and 5 are not independently landable in the planned order**, because what gives the cross-port gate its meaning is not who wrote the golden but that **both ports are byte-pinned to the same file**, which cannot hold for a golden they disagree on. The 2 stragglers are therefore **held for slice 5**. ⚠ The plan's own arithmetic was wrong: the gap is **7 goldens, not 1**. **Slice 5 wrote those two stragglers and inverted the contract**: the golden is now Rust's artifact (8 changed hex-float leaves across two files, exactly the predicted diff), the Rust byte census is **unconditional — no exemptions**, Python is the tolerance-gated side, and Python's regeneration mains now **refuse** all eighteen. ⚠ The hazard slice 4 flagged was confirmed by measurement rather than reasoning: the write left **both freeze-manifest gates green while the manifest pinned bytes that no longer existed**, so the biosphere unfreeze ceremony was run here rather than deferred — exactly two hashes moved. ⚠ The roster was **re-homed, not emptied**: the set is still true, but after the flip `Rust == golden` has one allowed answer, so the exemptions belong to the checker (`PORTS_DISAGREE` → `PYTHON_DIVERGES`) — *a symmetric name outlives its contract by exactly one slice*. Only **two** of the eighteen were loosened, because a tolerance cannot see a reduction-order change and the byte compare is the only Python-side gate that can; both scenarios keep a byte-gated sibling **at the other horizon**, and that is asserted rather than written down. ⚠⚠ A negative control killed the first design — converting only the two rostered modules left the roster's *heal* direction inert everywhere else, so every regression module now routes through one compare and one write choke point; *a policy with two implementations has one that is stale, and the control, not the review, said which*. ⚠⚠ The price of loosening was **measured, not argued**: a gross tamper is caught, a **last-nibble tamper (~1.4e-15) is not** — unavoidable, since it is indistinguishable from the permitted divergence, and the byte-exact backstop has moved to the side that owns the bytes. ⚠⚠ The plan's reason for re-measuring the bands was **false** (`measure_tier2_bands.py` is pure Python and shims CPython's own `math`, so the basis was never Rust-side); re-measuring reproduced every figure in the reference doc but found **`tiers.json` — the file that calls itself authoritative — two corrections behind** it. *Two prose halves of one contract can disagree, and the one declared authoritative is the one nobody re-reads.* ⚠ My first write-up of that called it an ungated band and added three gates; three equivalent gates already existed and the new ones were deleted before landing **Slice 6 re-anchored the biosphere manifest**, and its headline is **mixed authority, not "the manifest is Rust-anchored"**: the keys the reference tree can produce are now read from it, and **the file states the producer of every key, and why, in its own `_authority` block** — because by content roughly half of it cannot come from Rust at all (the science-gate census is a static read of pytest markers; the param-file list waits on a later slice). ⚠⚠ The predicted diff held exactly — **the only changes are that block and the header comment; no frozen value moved** — which is precisely why the slice needed a *discriminating* control rather than a green suite: renaming a flow in **Rust** moves the manifest, renaming the **Python** class leaves it byte-identical, and both turn the conformance gate red. *An interface slice's criterion must name a call site; a re-anchoring slice's must name a **direction**.* ⚠ The one key that could have broken CI for a non-science reason (the light-path fingerprint is gated **exactly**, not by tolerance) was **measured byte-identical between the two writers before** re-anchoring, not predicted. ⚠ The classification is keyed by **path**, because one scenario sits on both sides of the authorship line, and the golden rows are *checked against* the existing roster rather than restating it. Also closed: the manifest's golden hashes are now compared against the files on disk. ⚠ Two gates in one file stopped meaning the same thing — one is now a staleness check, its sibling still a two-port parity check — so the failure **message** had to branch, since the assertion fires identically either way. ⚠ A process cost worth the line: reverting a control with `git checkout` discarded the whole uncommitted slice in that file — **snapshot before running controls** — **Slice 7 did the same ceremony for the station manifest**, with the same result — the only diff is the classification block and the header comment, no frozen value moved — so the criterion was again a *pair* of renames, one per port, and the horizon axis got its own pair rather than inheriting the flow axis's. ⚠⚠ Its blocking item was an axis whose value is **empty**: the station has no aux process, so `[] == []` is every assertion about it, and this slice escalates that from *compared* to **written into the frozen contract by a regeneration**. The rename control is unrunnable there — nothing to rename — so the substitute is to **wire a throwaway one in** and drive the regeneration path: *when the value under test is empty, the control has to change the value, not the name.* ⚠ A gate that was **buildable was priced and refused**: the station's steps live in an ungated prose string whose numbers the reference tree does carry, but closing it needs a **new** structured key, i.e. a widening of the frozen surface — its own unfreeze, not a rider on a re-anchoring. *"The gate is buildable" and "the gate belongs in this slice" are different claims.* ⚠ Found while classifying: the station's 15-yr energy horizon **is** the biosphere's decade constant in the Rust tree, so the two contracts now share one reference-side value — one edit, two ceremonies. ⚠ One control had to be re-run because it reddened the target test **on the wrong assertion**: a stale classification pattern has to reach the file before the check that hunts stale patterns in the file can see it. *I had read one test and generalised*. **Slice 8 re-anchored the authoring manifest, and it is the contract where the flip is NOT free.** Again the predicted diff held exactly — only the classification block and the header comment moved, no frozen value — and again the criterion was a *pair* of renames, one per port. ⚠⚠ But the reason the two previous ceremonies were cheap does **not** transfer: both ports enumerate a **built registry** the same way, and this contract freezes a *platform*, which has no runtime object to interrogate. Python derives it by **language introspection** the reference does not have (a closed type union, a scan of the schema module, its field lists, a dict); Rust offers an `enum`, a `match` and a set of constants. So re-anchoring **traded a derived census for a partly hand-maintained one**, and the manifest now records per key which half is derived and which is a roster. *"The reference can express this contract" is a claim about the MECHANISM, not about the language — check whether the mechanism that made the last one cheap is even present.* The mitigation is that several rosters are **load-bearing** (they are the tables the parser rejects against, so dropping a name changes what the platform accepts) — ⚠ measured, and the **first measurement was wrong for a harness reason**: the Rust runner stops at the first failing target, reporting one red test where a full run reports fourteen across five. *Check that a control's runner reached every target, not merely that it exited non-zero* — the same class of error bit twice the same day, when a backgrounded suite inherited a directory change and reported **exit 0 / "no tests ran"**. ⚠ A frozen field that **nothing checked** turned up the only way such a thing can: a negative control was built for it and there was no test to redden. ⚠ A **third** anti-derived literal was found where the plan's own 23-vs-2 census never looked, because that census surveyed one gate. Because the reference side is weaker here the Python derivations were **kept with their meaning inverted in place** — the identical assertion now asks whether the *checker* has drifted — and one buildable forcing function was priced and deferred on slice 7's precedent | `post-roadmap-reference-flip.md` |

## The record

**One file per work item, in the same order as the index above.** Each file is that row's
Status cell, **moved verbatim** on 2026-08-12: the cell was one physical line, and the only
change is where it breaks — rejoining a file's body lines on single spaces reproduces the
cell character-for-character. Both digests are in `docs/context-budget.md` (rule 4), along
with why the table could not stay: 255,567 bytes in 32 physical lines defeats `Grep` (a
54,343-character row comes back as "one match"), `Read` (no way to page into a row) and
`git diff` (a one-word edit rewrites the whole line).

| Work | Record |
|---|---|
| **Development posture — the Rust-primary pivot** | [the record](log/rust-primary-pivot.md) |
| **Scope (B): the day-neutral habitat crop, validated vs LINTUL3 spring wheat** | [the record](log/day-neutral-crop.md) |
| **Scope (B): decomposer calibration — the carbon cluster moved above-range → top-of-range** | [the record](log/decomposer-calibration.md) |
| **The nitrogen-cycle FORM gap** (the decomposer calibration's named successor) | [the record](log/nitrogen-cycle-form.md) |
| **The canopy regulator** (the (C) diagnosis's named successor) | [the record](log/canopy-regulator.md) |
| **The chamber-scale diagnosis** (the upstream blocker with THREE witnesses) | [the record](log/chamber-scale.md) |
| **The acceptance gate** (the chamber-scale diagnosis's own conclusion, measured) | [the record](log/acceptance-gate.md) |
| **The science assertions get contract standing** (the acceptance gate's own finding 6, adjudicated) | [the record](log/acceptance-gate-standing.md) |
| **The humification split (a CUE)** — the soil-fractionation seam's named successor, **taken** | [the record](log/cue-humification.md) |
| **Soil carbon pool fractionation** (the chamber-scale diagnosis's named seam, taken — then RE-OPENED after the CUE, and REFUSED AGAIN ON A NEW LEG) | [the record](log/soil-fractionation.md) |
| **The crew-coupled loop** (the chamber-scale seam's standing alternative, taken) | [the record](log/crew-coupled-loop.md) |
| **The decade CO₂ guard, re-anchored** (the stem-only verdict's blocking contract question, answered by measurement rather than by choosing) | [the record](log/co2-guard-reanchor.md) |
| **Stem-reserve remobilization** (the user's own question: does the stem feed the seed?) | [the record](log/stem-reserves.md) |
| **Test-suite runtime** (tooling, not science) | [the record](log/test-suite-runtime.md) |
| The first authored habitat (`scenarios/algae_habitat.yaml`) | [the record](log/authored-habitat.md) |
| Tier 1: grow the flow registry (9 Power/Thermal/ECLSS flows + 3 loaders) | [the record](log/flow-registry-growth.md) |
| Tier 2: the grammar — `monod` | [the record](log/grammar-monod.md) |
| Bucket 3 scope (A): validation — diagnose + pin the oracle gap | [the record](log/validation-oracle-gap.md) |
| Bucket 3 scope (B): the full oracle match | [the record](log/oracle-match.md) |
| Bucket 2 (cont.): the export-fidelity hazard — **the one rationing cannot see** | [the record](log/export-fidelity.md) |
| Bucket 2 (cont.): **multi-rate authoring** — the chosen fix | [the record](log/multirate-authoring.md) |
| Bucket 2: the rationing gate — make the `dt` hazard loud | [the record](log/rationing-gate.md) |
| Bucket 3 scope (C): cite the no-oracle params | [the record](log/citation.md) |
| **The O₂ regulator's reversal is inside the freeze, not outside it** | [the record](log/o2-makeup-reversal.md) |
| **The direction gate — `ReversedFlowError`** | [the record](log/direction-gate.md) |
| **Tooling: the PDF-backed citation pins are green-by-skip on CI** | [the record](log/pdf-pins-ci.md) |
| **The second authored habitat — `scenarios/bioregenerative_station.yaml`** (the first habitat's finding #1, discharged) | [the record](log/bioregenerative-station.md) |
| **Potato — the first SECOND species** (stage 1 of 2) | [the record](log/potato-crop.md) |
| **The winter-wheat partition backfill** | [the record](log/wheat-partition-backfill.md) |
| **Root functional coupling** (the wheat refusal's named successor) | [the record](log/root-functional-coupling.md) |
| **Soil layers — the water side of root depth** | [the record](log/soil-layers.md) |
| **Tooling: the context budget** (the 2026-08-11 CLAUDE.md cut, re-opened because it did not hold) | [the record](log/context-budget.md) |
| **Re-deriving the soil water store from geometry** (the soil-layers build's own named finding, taken and BUILT) | [the record](log/soil-water-rebasing.md) |
| **Tooling: the record split** (that rule set's own rule 4, taken as its own piece of work) | [the record](log/record-split.md) |
| **The remaining water-stress curves** — `WSFD` BUILT, `WSFL` REFUSED | [the record](log/water-stress-curves.md) |
| **Sink-limited leaf expansion** — BUILT, BOUNDED by a second source, then BLOCKED under RK4 | [the record](log/leaf-expansion.md) |
| **Allocation headroom** — the leaf blocker re-diagnosed as a step-size bound, not a science defect | [the record](log/allocation-headroom.md) |
| **CO₂ enrichment** — the shipped step crosses the compensation point | [the record](log/co2-enrichment-margin.md) |
| **The CO₂ setpoint controller, priced** (the direction plan's Step 0, axis 2) | [the record](log/co2-controller.md) |
| **The step sweep** (the direction plan's Step 0, axis 1 — the last input to the step decision) | [the record](log/step-sweep.md) |
| **The step unfreeze** — `dt = 1` → `dt = ¼` (the sweep's decision, taken; the unit was the work, not the step) | [the record](log/step-unfreeze.md) |
| **The chamber CO₂ compensation-point band** (the guard that would have caught the step defect on day one) | [the record](log/co2-compensation-band.md) |
| **The parked leaf mechanism, re-measured at the shipped step** (finding 11's named condition, discharged) | [the record](log/leaf-remeasurement.md) |
| **The within-day light path — the plant breathes** (the user's charge; a mechanism that was already in the tree and could not fire) | [the record](log/gross-net-gas-exchange.md) |
| **The canopy this tree cannot grow** (the light path's own successor — the named candidate, measured and refused) | [the record](log/canopy-magnitude.md) |
| **The layered canopy + the leaf-thickness anchor** (the refused candidate, BUILT as honest physics on the user's call — and the mutual-shading loss the pair forced) | [the record](log/layered-canopy.md) |
| **The canopy's two uncited literals** (that build's own closing line — one was cited a file away, the other is a value question the shelf disagrees with itself about) | [the record](log/canopy-provenance.md) |
| **The reference flip — Rust becomes canonical** (target state B; eleven slices, eight landed — ⚠ target moved to C 2026-08-17) | [the record](log/reference-flip.md) |

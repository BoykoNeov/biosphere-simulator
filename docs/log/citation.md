## Bucket 3 scope (C): cite the no-oracle params

> One row of the record table in [`../post-roadmap-log.md`](../post-roadmap-log.md),
> split out on 2026-08-12 under rule 4 of
> [`../context-budget.md`](../context-budget.md). It was a single physical line of a
> markdown table; the only change is where it breaks. The fuller record is the plan
> doc this row names.

<!-- record-body: rejoin the lines below on single spaces for the original cell -->

COMPLETE — **a partial discharge, and a second structural finding**. The count was **29**,
not 15 (the "5" were *files*, holding 13 params; +2 in `water_cycle.yaml`, which scope (A)'s
7-PP/5-non-PP split omitted entirely; +4 station folded in). Outcome: **8 CITED, 14 DESIGN,
7 still TODO(cite) + a measured finding** — because half the values have no citation to find
(a *form* citation never licenses a *value*; citing a sizing choice IS the fabrication
failure mode). **The headline is not the citations:** the **decomposer side runs fast**
(litter decay ~1.5× above a 293-value global max; mineralization ~3.9× the Stanford & Smith
mean; microbial respiration **~2.5–28×**) — the mirror of scope (A)'s canopy collapse, on
the *return* side of the loop, and a new scope-(B) candidate. Also found: a **cross-kingdom
param copy** (herbivory `o2_half_saturation` mirrored from the microbial file, ~4× sharper
than animal physiology) and **two unverified citations** in the frozen tree (Dunn 2011 /
Divya 2009 — paywalled, never opened). No golden moved; 26/26 artifacts byte-identical.
**ROUND 2 (2026-07-16): the user supplied 10 of the 12 retrieval-list sources; the open
items were re-run against the primaries.** Round 1's *inferences* held to the digit
(Davidson's Km: predicted "~1200×/~3 orders/~150 µM/~58 %", actual **1210×/3.08/149 µM/58
%**), but its **flagship number moved**: Parton 1987 gives CENTURY's active-SOM max at
7.3/yr, so microbial respiration is **~2.5×** against *that* anchor — while RothC's BIO
(0.66/yr, now primary) keeps 28×. **The two lineages disagree ~11× on what "the microbial
pool" means, and that spread is the finding** — both recorded, because taking the flattering
2.5× is the re-anchoring trap this scope already refused. Retrieval list now **3 items, not
12** (Stanford & Smith, Dunn, Divya — all three still unread, so the miscitation risk
survives). **ROUND 3 (2026-07-20): the ceiling CONFIRMED, not inherited — and the scope
premise was false a THIRD time.** "Read the 3 unread sources" reads like a reading task; it
is a **retrieval** task, and none of the three is in `sources/` (they were never supplied).
One bounded pass, **legitimate channels only** (pirate mirrors excluded on principle —
`sources/` is gitignored *because* the corpus is copyrighted): all three still closed, DTU
Orbit still metadata-only, so the **miscitation risk in the frozen tree stands uncleared**.
The one real result: **[E] Smith 1987, a Current Contents "Citation Classic" — a
first-person retrospective by [A]'s own second author**, open access, found because round 1
never looked for one. **It gives no k**, so the 3.9× still rests on the [C]-quoting-[A]
chain and `mineralization_rate` is still the weakest-provenance member — but the FINDING's
*premise* (S&S's rates are **already** optimal-condition potential rates, 35 °C / field
capacity, 39 soils, 30 wk) is now **first-hand**, so the chain is **broken for the
conditions and survives for the number**. Read as **page images** after text extraction
returned garbage — round 2's discipline holding, with the render legible this time. **The
meta-finding takes an EIGHTH instance, and it is this scope's own**: `mineralization.yaml`
said *"Retrieval of [A] remains the only upgrade path"* — true of the **value**, too broad
as written, and falsified by a document round 2 never searched for. Round 2 concluded "no
first-hand k exists in the corpus" (correct) and wrote down "only [A] can upgrade this"
(larger, and false); **the qualifier gets dropped and the fact reads like the rule**.
**ROUND 4 (2026-07-20): THE RISK MATERIALIZED — the user supplied 2 of the 3, and one was a
CONFIRMED FALSE ATTRIBUTION in the frozen tree.** Rounds 1–3 could only *record* the hazard
of a citation nobody had opened; round 4 measured it. **Dunn 2011 contains NO self-discharge
figure at all** — "self-discharge" appears **once**, qualitatively, and "%/month" **zero**
times — yet `self_discharge.yaml` had attributed "Li-ion self-discharge is ~2-3 %/month
[A][B]" to it since Phase 5. **Established by full-text extraction, not by reading page
images**, and that is the round-3 lesson one step on: a verbatim quote is ungated, but **an
ABSENCE claim is the one kind of claim skimming structurally cannot establish** — "I read it
and didn't see a number" is not a finding. **The round-1 skeptical read predicted it with no
retrieval at all** ("a grid-storage REVIEW is an improbable place for a self-discharge
constant"), which makes *locus plausibility* a cheap check worth running on every remaining
unopened citation — **but the identical read applied to Divya got the OPPOSITE result**, so
it forecasts absence, never presence. **Divya 2009 DID carry the by-chemistry %/month table
round 2 predicted** — round 2's *retrieval* prediction confirmed exactly — **and that same
table FALSIFIED FINDING 1**, whose "vendor folklore, not literature / **never** a canonical
%/month constant" is false on both counts (Divya is peer-reviewed Elsevier and tabulates it
for 8 chemistries). Recorded as a **NARROWING, not a "survives in stronger form"** — an
advisor catch, because the confirmed prediction is exactly what tempts you to blur the
falsification *on the same page*. What survives is only the primary/review split: Divya's
Table 1 is cited to refs [10–12], **three bare URLs**, with no method/cell/temperature — a
**provenance observation, not an intent claim**. Its Li-ion figure is **1 %/month**, so ours
(2.59) is **~2.6× above the only number it was cited for**, and three accessible sources now
bracket ours from below while disagreeing with each other by an order of magnitude. **The
value is UNCHANGED but its WARRANT changed, and the two are now kept apart deliberately**
(running them together is how a falsified citation survived four rounds under a
still-defensible conclusion): *the value may stand* (conservative + the attractor proved
magnitude-independently) and *its original justification is falsified* are both true, and
**(1) does not rescue (2)**. Left **open and deliberately not pre-answered**: may a FROZEN
value rest on "conservative + magnitude-independent test" alone when every accessible source
disagrees and its cited sources never supported it? If no, the fix is **recalibration =
scope B** (moves power goldens, cascades to station) — a user decision. `charge.yaml`'s
parallel attributions **both held** (Dunn's Table 1 gives 90–94, so our old "~0.90-0.95"
overstated the bound, and it is EPRI's table reproduced) — **the same two papers, cited in
two files by the same author at the same time, one fabricated and one sound: the unit of
trust is the CLAIM, not the paper**. ⚠ These are `power/` params ⇒ the **STATION** contract;
the reflex to regenerate the *biosphere* manifest (round 3's) was caught before it shipped a
stale station hash. Retrieval list **12 → 3 → 1** (Stanford & Smith only). **⚠ AND ROUND 4
COMMITTED THE META-FINDING ITSELF BEFORE CATCHING IT — a NINTH instance, self-inflicted, one
turn after writing the lesson down.** The opening grep listed **four** files holding
Dunn/Divya; the sweep fixed the **two** `power/` ones and declared done, leaving
`mineralization.yaml`'s round-3 sentence *"Dunn 2011 and Divya 2009 were re-attempted and
are still closed"* standing **one day after opening both** — i.e. round 4 falsified its own
sibling round's prose and shipped it. Caught on the closing advisor call, from evidence
*already in the transcript*. **A stale negative result is worse than a stale positive: it
SUPPRESSES the next search** ("recorded so the search is not repeated" was the line's own
stated purpose). Two rules fall out: **a retrieval ceiling is a fact about one afternoon's
access, never a property of the source** — date it, never write it flat; and **when a grep
hands you N sites, the sweep is not done at N−2** — the cross-domain refs needed the same
care as the in-file ones and the tool had already pointed at them. So the real diff is **4
param hashes across BOTH manifests** (station: `charge`, `self_discharge`; biosphere:
`mineralization`, `decomposition`) — comment-only edits **do** move the normalized hash, so
the follow-up was a second honor-system unfreeze, not free. `decomposition.yaml` was the
*opposite* case and is now the **positive control**: its FAO-quoting-Olson hedge became a
**measured matched pair** — same secondary-quoting-primary shape, opposite outcome, both
verified — which is why round 4's finding must NOT be generalized into distrust of every
such chain. No value/golden/code moved; both `biosphere_params.txt` and `sibling_params.txt`
byte-identical. Scope (C) is best read **CLOSED with a documented residual risk** (now: 1
secondary-mediated number + 1 param correctly attributed to nothing, with an open scope-B
question ⚠ **— that question is ANSWERED as of 2026-08-10; see the end of this row**),
blocked on **retrieval, not effort**. Auditing the ~15 unopened corpus sources was
**considered and rejected as a scope substitution** — round 2 called them "mostly scope-B
science", so opening them under a scope-C banner *is starting scope B*, a user decision; the
narrow legitimate exception left on the table is the **2 battery texts** (Braun, SAE) for
`self_discharge_rate` — ✅ **TAKEN IN ROUND 6; nothing is left on the table.** **ROUND 5
(2026-07-20): THE RETRIEVAL LIST REACHED ZERO (12 → 3 → 1 → 0)** — the user supplied
Stanford & Smith 1972, the item that had carried `mineralization_rate` as the
**weakest-provenance member** of the decomposer finding through four rounds. **Two results,
and the second is what justified the round.** (1) **The chain HELD**: [C] Schomberg 2009
reproduces [A]'s `0.054 ± 0.009 wk⁻¹` **correctly to the digit**. That is the scope's
**third** secondary-quoting-primary chain opened first-hand, making the record **2 sound
(FAO→Olson, Schomberg→S&S) : 1 fabricated (Dunn)** — so round 4's finding stays a **risk
flag, not a verdict**, exactly as it was recorded. ⚠ And **what separates the failure is not
the chain shape**: both sound cases transmit a number the primary actually prints, while
Dunn was cited for a number that paper never contained — a **locus** failure, not a
**transmission** failure, catchable *only* by opening the cited paper. (2) **The primary
gave what no secondary republishes, and the finding got HARDER**: [A]'s Table 3 has all **39
per-soil k values, spanning 0.035–0.095/wk**, so ours (0.21/wk) is **~2.2× above the FASTEST
SINGLE SOIL of the 39** — a first-hand hard bound that **replaces round 3's soft "~1.2×
above [C]'s fastest of 43"**. Direction unchanged, force roughly doubled. Also newly
visible: **0.054 is not a plain mean** but a weighted average of **29 pooled regressions
after 10 divergent soils were excluded** — the number was exact and the chain still lost
something. The scan column was **VERIFIED BY ARITHMETIC, not eye** — four of [A]'s own prose
counts had to hold simultaneously (39 entries; exactly 3 below 0.044; exactly 7 above 0.069;
residual 29 spanning exactly 0.044–0.069). **The live counter-reading is now POOL IDENTITY,
not provenance** (advisor catch — my draft had only the weaker temperature caveat): S&S
measured **soil** organic N₀; our `litter_n` is **fresh dead-plant N**, which mineralizes
faster, so part of the 2.2× may be a litter-vs-soil mismatch. **Recorded and REFUSED**,
exactly as `decomposition.yaml` refuses the DPM/RPM labile-fraction re-read — *redefining
what the pool MEANS to make a citation fit is a semantic model change, not a citation edit*
— and it cannot explain why the **whole decomposer cluster** runs fast on the same side of
the loop. **Two stale sentences found, both ours**: round 3 mis-transcribed [E]'s schedule
as "four, four, and **six**" where the page says "**four**" — round 3's version **sums to 32
inside a sentence saying "for 30 weeks"**, i.e. the contradiction was in our own quote, and
round 3 ran exactly that arithmetic check on `[B]`'s mangled table **in the same file** but
not here ⇒ **a quote check verifies characters; only arithmetic verifies numbers**, and the
careful-looking re-read caught the things that *looked* odd while missing the digit that
read fluently. And `n_senescence_rate`'s *"retrieving the [B] monograph is the only clean
path"* — **[B] was retrieved and read in round 2 and had already failed**; it survived
because every later round arrived via the *other* param, so **nothing routed attention to
it**. Its honest status is retrieval **EXHAUSTED, not blocked** (a fact about the
literature, not about access). **Round 3's "recorded so the search is not repeated" block is
now falsified IN FULL** — three items, three openings, ~a day apart each — confirming round
4's rule three times over. ⚠ **Scope-B pressure went UP, not down**: closing the citation
question **sharpened** the calibration question. Correcting a self-contradiction this row
carried: the real manifest diff for round 4 was **4 hashes across BOTH manifests** (the tail
said "exactly 1", round 3's leftover); round 5's is **exactly 2, biosphere-only**
(`mineralization`, `decomposition`) — confirmed by `grep -l` rather than assumed, the mirror
of round 4's reflex error. Residual risk is now **1 param correctly attributed to nothing**
(`self_discharge_rate`). No value/golden/code moved; `biosphere_params.txt` byte-identical.
**ROUND 6 (2026-07-20): THE INCREMENT ON THE TABLE, TAKEN — and it FALSIFIED A SENTENCE OF
OUR OWN.** The first round here **not blocked on the user supplying anything**: the 2
battery texts had sat in `sources/` since round 1, named as the one legitimate exception by
round 3, sharpened by round 4, left standing by round 5. **One hit, one confirmed absence.**
(1) **Braun Table 1.5 (p. 37) gives Li-ion `5 %/month` — the FIRST accessible source ABOVE
ours** (2.592 = 0.518× it; Divya's 1 = 0.2× it), which **falsifies FINDING 3's closing
sentence** (*"every one of them puts the true rate BELOW 2.59, and ours is above the highest
of the three"*). Ours is **BRACKETED** (1 … 2.592 … 5), not above everything. ⚠ **NARROWING,
NOT VINDICATION** — [E] is **not better provenance than [B]**: a *textbook reprinting a
conference-proceedings table* (Bocklisch 2015, Energy Procedia) with no
method/cell/temperature/SOC — the **same gap** as Divya's three bare URLs, so it is a
**fourth tabulated number, not a measurement**, and does not bind. What it *does* do is turn
FINDING 1's surviving half from argued into **observed, four papers, two per side**: the
sources that **TABULATE** the constant ([B] 1, [E] 5) **disagree 5× with each other and
bracket us**; the sources that **MEASURE** it ([C] ~0.4 %/30 d @25 °C, [D] 0.02–0.65
implied) report **no %/month at all and BOTH still sit BELOW us** — *a genre convention, not
a determined quantity*. The reading was guarded because **Table 1.5 is printed ROTATED**
(off-by-one-column is the live hazard): the guard was a **whole-column plausibility check**
(200–350 W h/L, 2000–7000 cycles, 80–85 %, 3–5 ms — all sane for Li-ion, none sane for the
neighbours), round 5's "only arithmetic verifies numbers" applied one column over.
**Page-image reading was FORCED, not chosen**: extraction finds the cell (`%/month` ×1) but
a font-encoding failure renders **every numeral in that table as U+FFFD** — the mechanical
channel failing on exactly the characters at issue is the one case where round 3's method is
the *right* tool. (2) **Turner/SAE contains NO self-discharge rate for any chemistry — a
CONFIRMED ABSENCE over all 98 pages, and the METHOD is the finding.** The obstacle beat
round 4's: the PDF is a **scan with NO TEXT LAYER** (`pdftotext` → **98 bytes for 98
pages**), so extraction — the very tool round 4's lesson prescribes — was unavailable. The
text layer was **manufactured**: every page rendered at 200 dpi and OCR'd (Windows Media
OCR), **output quality-checked verbatim against a page read by eye BEFORE any count was
trusted** — not ceremony, since *an absence claim built on unverified OCR launders a tooling
failure into a finding*. Result over ~282k chars: `%/month` **×0**, `float current` ×0,
`leakage` ×0, `self-discharge` ×3, `per month` ×2 (both **DC-fast-charger usage
frequency**). ⚠ **The hit that mattered was not a keyword hit**: `Rsd`, "a resistance
representing the cell self-discharge" — **`Rsd·CQ` is a time constant, i.e. `1/k`, our param
in disguise** — which a keyword count files as "structural, no number" and walks past.
Chased specifically; it lives only in the EKF state equations, is **absent from the circuit
diagram** (Figure 8: R0/R1/R2/C1/C2), the paper has **no parameter table**, and the nearby
"table" tokens are Simulink lookup-block names (`R_table_1Temp`). Locus plausibility was
right a second time and **again is not why it is recorded** — all 98 pages were checked
mechanically anyway, because *the identical read on [B] in round 2 got the opposite result*.
**⚠ THE META-FINDING TAKES A TENTH INSTANCE, and it is ROUND 4'S OWN RULE ONE STEP
SIDEWAYS**: round 4 coined *"a retrieval ceiling is a fact about one afternoon's ACCESS,
never a property of the source"* — then, **in the same document, in the same round**, wrote
a **BRACKETING** claim flat. A fact about *three papers then in hand* became a property of
the literature, and **the qualifier is what got dropped** (round 3's *"[A] is the only
upgrade path"*, round 5's *"[B] is the only clean path"*, same mechanism). Generalized:
***any* "every source says X" is a statement about YOUR SHELF** — date it and scope it; this
one expired in **four days**. **The scope-B question is RESTATED, NOT ANSWERED** ⚠ **—
ANSWERED 2026-08-10, see the end of this row.**  Its premise (*"every accessible source
disagrees with it"*) is now false. Restated: may a frozen value rest on "conservative +
magnitude-independent test" when it is **bracketed** by two review tables that disagree 5×
with each other, sits **above both sources that actually measured** it, and was **originally
attributed to two papers that never supported it**? **The case for leaving it alone got
STRONGER, the case for recalibration WEAKER, neither is settled** — the clause that still
bites is the *measured*-literature one. Retrieval for this param is now **EXHAUSTED, not
blocked** (round 5's distinction), dated to 2026-07-20. Residual risk **unchanged in kind,
reduced in force**: still *"1 param correctly attributed to nothing"*, no longer *"a param
every accessible source contradicts"*. Manifest diff is **exactly 1 hash, STATION-only**
(`self_discharge.yaml`) — the third application of round 4's reflex error / round 5's `grep
-l` fix; the **authoring** manifest also names `self_discharge` (flow-type + param *names*,
which a comment edit cannot move) and was **regenerated to prove no diff rather than
argued**. No value/golden/code moved; `sibling_params.txt` byte-identical. **⇒ ROUND 7 / THE
ANSWER (2026-08-10, the user's call): `self_discharge_rate` is RETAGGED `TODO(cite)` →
`DESIGN`, VALUE UNCHANGED — "leave the value, fix the note."** The scope-B question rounds 4
and 6 refused to pre-answer is **answered**, and the deciding reason was neither of the two
this file had been leaning on: **recalibration had NO TARGET TO AIM AT.** The only two
sources that *tabulate* this constant give **1** and **5** %/month, disagree **5× with each
other**, and **neither measured anything** (three bare URLs; a reprinted conference table) ⇒
moving would trade *"an uncited value"* for *"one of two guesses, arbitrarily picked"* **and
pay a power→station golden cascade for it** — and picking the nearer of two non-measurements
*because* it is nearer is the fitted-comparison shape this project refuses elsewhere. The
two supporting reasons are the ones already on record (the number is **not load-bearing** —
the SOC attractor is proved magnitude-independently by the two-run contraction test; and its
error is in the **safe direction**). ⚠ **NOTHING IS DISCHARGED BY A RETAG, and both
survivors are recorded rather than closed**: the value still sits **ABOVE both sources that
actually MEASURED** the quantity — a **RESIDUAL**, now an *owned design choice* instead of
an open debt, and **a source that MEASURES a Li-ion %/month figure is grounds to revisit**
(retrieval exhausted **as of 2026-07-20**, dated by this scope's own rule) — and the
**miscitation** stands as history: relabelling records where the number came from, it does
not make four phases of a false attribution harmless. ⚠ **`DESIGN` MEANS "no source binds
the VALUE", NEVER "no source was sought"** — the entire six-round negative retrieval record
is **KEPT, not trimmed**, for `docs/retired/mineralization.yaml`'s reason (a stale negative
result suppresses the next search; six rounds of it is the difference between *"nobody
looked"* and *"the quantity is not reported this way"*). **Scope B is NOT closed, only not
taken**, at the same price. Manifest diff is **exactly 1 hash, STATION-only** — the
**fourth** honor-system provenance unfreeze, and round 5's `grep -l` fix applied a fourth
time: the **authoring** manifest also names `self_discharge` (flow-type + param *names*,
which a comment edit cannot move) and was **REGENERATED to prove no diff rather than
argued**, as was `sibling_params.txt` (both byte-identical). Science gates: station
`science_bands`/`liveness_floors` structurally untouched (neither names this param; no bound
moved). No value/golden/code moved; `git diff src/` is one `source:` string.
`docs/plans/post-roadmap-citation.md`
